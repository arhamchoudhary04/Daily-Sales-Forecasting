# Serving dependencies only.
# Training extras: requirements-train.txt   Tests: requirements-dev.txt
# Upper bounds keep a new major from landing silently in a build.

# --- API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- data / modeling ---
pydantic>=2.7,<3.0
numpy>=1.26,<3.0
pandas>=2.2,<4.0
xgboost>=2.0,<4.0
joblib>=1.4,<2.0

# --- foundation model (zero-shot forecasting) ---
# torch is only lower-bounded on purpose: the Dockerfile and CI pre-install the
# CPU wheel, and a tighter pin makes pip pull the much larger CUDA build.
torch>=2.2
chronos-forecasting>=2.0,<3.0
